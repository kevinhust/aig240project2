#!/usr/bin/expect -f
set password "123"
set host "192.168.64.4"
set user "jetauto"

spawn ssh -o StrictHostKeyChecking=no $user@$host "source /opt/ros/melodic/setup.bash; rosversion -d; ls -la ~"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
